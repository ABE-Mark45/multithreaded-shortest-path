#pragma once
#include "utils/types.h"

namespace graph_constants {
constexpr graph::DistanceType kInvalidDistance = -1;
constexpr graph::DistanceType kZeroDistance = 0;
}  // namespace graph_constants